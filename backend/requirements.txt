# Crave App Backend — Requirements
# Python 3.14+ compatible (uses --pre for pydantic)
# Instalación: pip install --pre -r requirements.txt

--pre
pydantic
pydantic[email]
fastapi
uvicorn[standard]
python-jose[cryptography]
bcrypt
python-dotenv
python-multipart
google-generativeai
httpx==0.27.2
email-validator
